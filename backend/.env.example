# 本地开发用。复制为 backend/.env:
#
#     cp .env.example .env
#
# sqlx 的编译期 SQL 校验和 sqlx-cli 都从这里读连接串。用的是
# scripts/init_db 起的那个开发数据库,不是生产配置。
#
# 生产部署不用这个文件,配置见 docs/configuration.md。
DATABASE_URL="postgres://postgres:password@localhost:5432/lab_inventory"
